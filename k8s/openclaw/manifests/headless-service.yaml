apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
  labels:
    app.kubernetes.io/name: openclaw
spec:
  clusterIP: None
  ports:
    - port: 18789
      targetPort: gateway
      name: gateway
  selector:
    app.kubernetes.io/name: openclaw
