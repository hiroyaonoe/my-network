server:
  service:
    type: LoadBalancer
    loadBalancerIP: "10.5.0.1"
    annotations:
      external-dns.alpha.kubernetes.io/hostname: argocd.internal.onoe.dev
  certificate:
    enabled: true
    domain: argocd.internal.onoe.dev
    issuer:
      name: letsencrypt
      kind: ClusterIssuer
      group: cert-manager.io

global:
  monitoring:
    enabled: true
    serviceMonitor:
      enabled: true

configs:
  params:
    server.insecure: false
  cm:
    url: https://argocd.internal.onoe.dev
