apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rook-csi-rbd-node
  namespace: rook-ceph
spec:
  refreshInterval: 1h0m0s
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  target:
    name: rook-csi-rbd-node
    creationPolicy: Owner
  data:
    - secretKey: userID
      remoteRef:
        key: rook-ceph/rook-csi-rbd-node
        property: userID
    - secretKey: userKey
      remoteRef:
        key: rook-ceph/rook-csi-rbd-node
        property: userKey
